{
  "command": "getsebool",
  "description": "Show SELinux boolean values",
  "global_options": [
    { "short": "-a", "description": "Show every SELinux boolean" }
  ],
  "arguments": [
    {
      "name": "boolean",
      "description": "SELinux boolean",
      "required": false,
      "multiple": true,
      "type": { "type": "Dynamic", "data": { "provider": "selinux.boolean" } }
    }
  ]
}
