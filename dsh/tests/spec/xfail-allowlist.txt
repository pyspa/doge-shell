# XFAIL allowlist: one contract case ID per line.
#
# A spec case with `xfail = "..."` MUST be listed here, and every entry here
# MUST have a matching `xfail` case. This keeps new regressions from being
# hidden behind an ad-hoc xfail and forces stale markers to be removed once
# the bug is fixed (the fix surfaces as XPASS -> suite failure).

background.pipeline-in-background
pipeline.assignment-stage-parent-isolation
